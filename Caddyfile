{$SITE_DOMAIN} {
    @backend path /api /api/* /admin /admin/* /static/* /media/*
    handle @backend {
        reverse_proxy backend:8000
    }

    handle {
        reverse_proxy frontend:3000
    }
}
